class Person {
    name: string;
    code: number;
    cardId: number;
    constructor(completeName: string,code: number, cardId: number) {
      this.name = completeName;
      this.code = code;
      this.cardId = cardId;
    }
  }